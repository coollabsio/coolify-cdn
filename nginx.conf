server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html index.json;

    # Enable ETags (explicitly set)
    etag on;

    # Add CORS headers for CDN usage
    add_header Access-Control-Allow-Origin "*" always;
    add_header Access-Control-Allow-Methods "GET, HEAD, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept" always;

    # Cache control headers
    add_header Cache-Control "public, must-revalidate" always;

    # Redirect 404s to coolify.io
    error_page 404 = @redirect_to_coolify;

    # Proper MIME type for JSON files
    location ~* \.json$ {
        add_header Content-Type application/json;
        add_header Access-Control-Allow-Origin "*" always;
        add_header Cache-Control "public, must-revalidate" always;
        etag on;
    }

    # Redirect root to coolify.io
    location = / {
        return 302 https://coolify.io;
    }

    # Default location
    location / {
        # Handle OPTIONS requests for CORS preflight
        if ($request_method = OPTIONS) {
            return 204;
        }
        try_files $uri $uri/ =404;
    }

    # Named location for 404 redirect
    location @redirect_to_coolify {
        return 302 https://coolify.io$request_uri;
    }

    # Disable access logs for health checks (optional)
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}
